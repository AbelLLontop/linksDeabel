export const ButtonsForm = ({ocultar}) => (
    <div className="buttons">
      <div onClick={ocultar} className="btn btn-cancel">Cancel</div>
      <button className="btn btn-save">Save</button>
    </div>
  );
  